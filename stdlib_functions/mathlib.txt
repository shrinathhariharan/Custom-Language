import "math"

dec result = math.pow(2.0, 3.0) // power

dec absResult = math.abs(-5.5) // absolute value

dec sqrtResult = math.sqrt(16.0) // square root

dec sinResult = math.sin(0.0) // trigonometric sine

dec cosResult = math.cos(0.0) // trigonometric cosine

dec tanResult = math.tan(0.0) // trigonometric tangent

dec logResult = math.log(2.718281828) // logarithmic function with base e

dec log10Result = math.log10(100.0) // logarithmic function with base 10

dec expResult = math.exp(1.0) // e to the power of x

dec ceilResult = math.ceil(3.2) // ceiling of a number

dec floorResult = math.floor(3.8) // floor of a number

dec roundResult = math.round(3.5) // rounds to nearest whole number
