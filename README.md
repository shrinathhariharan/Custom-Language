**Custom Language**


A custom language compiled in c++. Supports variable types: int (integer), dec (double/float), str (string), and bool (boolean). Supported loops: for loops and while loops. Supports fixed arrays, functions, and comments

Uses the abstract syntax tree to parce the code into C++ Node objects